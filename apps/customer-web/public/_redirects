# Belt-and-suspenders: any leftover request to the bundled onnxruntime-web
# WASM (we strip it from dist/ at build time — see vite.config.js) is
# redirected to jsDelivr's public CDN, so it never bills Netlify bandwidth.
# imgly handles its own WASM fetching from staticimgly.com at runtime, so
# this rule is purely defensive against stale cached references and bots.
/assets/ort-wasm-simd-threaded.jsep-*.wasm  https://cdn.jsdelivr.net/npm/onnxruntime-web@1.21.0/dist/ort-wasm-simd-threaded.jsep.wasm  302!

# SPA fallback (must come last)
/*    /index.html   200
